<?xml version="1.0" encoding="utf-8"?>
<odoo>


    <record model="ir.rule" id="sale_stk_report_comp_rule">
        <field name="name">Sales Report Sensotek multi-company</field>
        <field name="model_id" ref="model_sale_report_sensotek"/>
        <field name="global" eval="True"/>
        <field name="domain_force">[
            ('company_id', 'in', user.company_rstk_ids.ids),
            '|',
            ('partner_id', 'in', user.partner_rstk_ids.ids),
            ('partner_id.parent_id', 'in', user.partner_rstk_ids.ids),
            ]
        </field>
    </record>


  <!--
    <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>

    <record id="sale_order_report_personal_rule" model="ir.rule">
        <field name="name">Personal Orders Analysis</field>
        <field ref="model_sale_report" name="model_id"/>
        <field name="domain_force">['|',('user_id','=',user.id),('user_id','=',False)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
    </record>

    <record id="sale_order_report_see_all" model="ir.rule">
        <field name="name">All Orders Analysis</field>
        <field ref="model_sale_report" name="model_id"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman_all_leads'))]"/>
    </record>

    -->


</odoo>