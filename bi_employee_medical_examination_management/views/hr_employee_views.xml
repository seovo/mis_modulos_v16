<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="act_medical_examination_employee" model="ir.actions.act_window">
        <field name="name">Medical Examinations</field>
        <field name="res_model">hr.employee.medical.examination</field>
        <field name="view_mode">list,form</field>
        <field name="context">{
                'search_default_employee_id': [active_id],
                'default_employee_id': active_id,
            }
        </field>
    </record>

    <record id="payroll_hr_employee_view_form" model="ir.ui.view">
        <field name="name">payroll.hr.employee.view.form</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button name="%(act_medical_examination_employee)d" class="oe_stat_button" icon="fa-medkit" type="action">
                    <field name="medical_examination_count" widget="statinfo" string="Medical Examinations"/>
                </button>
            </div>
        </field>
    </record>

</odoo>